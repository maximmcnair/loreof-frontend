{
  "name": "loreoffrontend",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
  },
  "devDependencies": {
    "gulp": "^3.5.6",
    "gulp-stylus": "~1.0.2",
    "nib": "~1.0.3"
  },
  "scripts": {
    "lint": "./node_modules/.bin/jshint .",
    "test": "./node_modules/.bin/istanbul test ./node_modules/.bin/_mocha -- --recursive -R spec -t 5000",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --recursive -R spec -t 5000",
    "start": "gulp"
  },
  "contributors": [
    {
      "name": "Maxim McNair",
      "email": "mail@maximmcnair.com"
    }
  ],
  "license": "Closed"
}
